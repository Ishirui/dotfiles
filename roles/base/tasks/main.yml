- include_vars: user.yml
- include_vars: fs.yml
- include_vars: packages.yml

# Packages first: everything below assumes the software it configures exists.
# The package set itself is declared in home/dot_config/metapac/groups/.
- include_tasks: 00-bootstrap.yml
- include_tasks: 01-packages.yml

# System-level setup
- include_tasks: 02-btrfs.yml
- include_tasks: 03-fs.yml

# Applications that carry configuration Ansible still owns
- include_tasks: 04-syncthing.yml
