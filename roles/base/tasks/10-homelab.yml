# Homelab tooling. Note: the syncthing stack lives in 05-syncthing.yml since it
# carries its own configuration.

- name: Homelab | Install tools
  tags: software,homelab,tools,cli,gui
  become: true
  become_user: aur_builder
  kewlfft.aur.aur:
    name:
      - wireguard-tools
      - bitwarden
