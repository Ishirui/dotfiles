# Miscellaneous GUI system tools without dedicated config.

- name: GUI tools | Install GUI system tools
  tags: software,tools,gui
  become: true
  become_user: aur_builder
  kewlfft.aur.aur:
    name:
      - partitionmanager # Should be installed already
      - filelight # Ditto
      # - cheese # Camera utility
      - helvum # Audio patch panel
      - qbittorrent
      - wireshark-qt
      - virt-manager
